package main.java.org.example;import com.gamestudio.tictactoe.Board;import java.util.Scanner;public class TicTacToe {    private Board board;    private char currentPlayer;    public TicTacToe() {        board = new Board();        currentPlayer = 'X'; // Spieler X beginnt traditionell    }    public void playTurn() {        Scanner scanner = new Scanner(System.in);        int row, col;        System.out.println("Current Player: " + currentPlayer);        // Schleife läuft so lange, bis der Spieler einen gültigen Zug macht        while (true) {            System.out.print("row (0-2): ");            row = scanner.nextInt();            System.out.print("column (0-2): ");            col = scanner.nextInt();            // Versuche das Symbol zu setzen (Validierung geschieht im Board)            if (board.placeSymbol(row, col, currentPlayer)) {                break; // Zug war erfolgreich, Schleife verlassen            } else {                System.out.println("This move is invalid or the cell is already taken! Try again.");            }        }        // Nach einem erfolgreichen Zug wechselt der Spieler (Vorbereitung)        switchPlayer();    }    private void switchPlayer() {        currentPlayer = (currentPlayer == 'X') ? 'O' : 'X';    }    public static void main(String[] args) {        TicTacToe game = new TicTacToe();        // Für diese erste User Story simulieren wir einfach ein paar Runden hintereinander        System.out.println("--- TicTacToe Game Started ---");        game.playTurn();        game.playTurn();    }}